# Required libraries
requests>=2.25.1

# Optional: Uncomment if you want cross-platform colored terminal output
# colorama>=0.4.4

# Optional: Uncomment if you plan to use Python-nmap for advanced port scanning
# python-nmap>=0.7.1

# Optional: BeautifulSoup for HTML content inspection (future use)
# beautifulsoup4>=4.10.0

# Optional: lxml for fast HTML/XML parsing (used with BeautifulSoup)
# lxml>=4.6.3
